<%= link_to '+新的見証', new_witness_path %>
<hr/>
<div class="row">
  <% @witnesses.each do |witness| %>
      <div class="col-lg-4 col-sm-6 col-xs-12">
        <a href="<%=witness_path(witness)%>">
          <b><%= witness.title %></b>
          <img src="<%= witness.photo %>" class="thumbnail img-responsive">
          <%= link_to '修改', edit_witness_path(witness), :class => "btn btn-warning" %>
          <%= link_to '刪除', witness_path(witness), :class => "btn btn-danger", :method => :delete, :data => { :confirm => "確定刪除?" } %>
       </a>
      </div>
  <% end %>
</div>
